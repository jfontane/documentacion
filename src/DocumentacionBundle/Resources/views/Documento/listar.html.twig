{% extends "@Documentacion/layout.html.twig" %}
{% block title %}Listado de Documentos{% endblock %}
{% block pageid 'admin' %}

{% block header %}

<ul class="navbar-nav mr-auto">
  <li class="nav-item">
    <a class="nav-link" href=""><i class="material-icons">home</i> <span class="sr-only">(current)</span></a>
  </li>
<li class="nav-item active">
<a class="nav-link" href="">Organismos <span class="sr-only">(current)</span></a>
</li>
<li class="nav-item">
<a class="nav-link" href="">Representantes</a>
</li>
<li class="nav-item">
<a class="nav-link" href="">Declaraciones</a>
</li>
<li class="nav-item">
   <a class="nav-link" href="">Importacion</a>
</li>
<li class="nav-item">
   <a class="nav-link" href="">Usuarios</a>
</li>
</ul>
<ul class="list-inline custom-icon-middle">
<li class="list-inline-item">
<a href="#" class="dropdown-toggle" id="dropdownMenuLinkPerson" data-toggle="dropdown"
aria-haspopup="true"
aria-expanded="false" title="Usuario logeado"><i class="material-icons">person</i></a>
<div class="dropdown-menu custom-dropdown-menu-right" aria-labelledby="dropdownMenuLinkPerson">
<a class="dropdown-item" href="">Cambiar Contraseña</a>
</div>

</li>
<li class="list-inline-item"><a href="" title="Salir"><i class="material-icons">exit_to_app</i></a>
</li>
</ul>

{% endblock %}

    {% block body %}
        {# CONTENIDO ORIGINAL DEL ARCHIVO Y SIN LAS ETIQUETAS <html><body> � </body></html> #}
        <div class="grid_16">
            <table class="table table-bordered">
                <thead class="thead-dark">
                    <tr>
                        <th>ID</th>
                        <th>Cuil</th>
                        <th>Archivo</th>
                        <th>A&ntilde;o</th>
                        <th>Mes</th>
                        <th>Descripcion</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>

                {% for documento in documentos %}
                                        <tr>
                                            <td>
                                                {{ documento.id }}
                                            </td>
                                            <td>{{ documento.cuil }}</td>
                                            <td>{{ documento.archivo }}</td>
                                            <td>{{ documento.periodoAnio }}</td>
                                            <td>{{ documento.periodoMes }}</td>
                                            <td>{{ documento.Descripcion }}</td>
                                            <td>
                                                <a href="{{path('documento_descargar',{'id' : documento.id })}}">Ver</a>&nbsp;&nbsp;
                                                <a href="">Editar</a>&nbsp;&nbsp;
                                            </td>
                                        </tr>
                                    {% else %}
                                        <tr>
                                            <td colspan="7">No existe ningun Documento</td>
                                        </tr>
                                    {% endfor %}



                </tbody>
            </table>
        </div>
    {% endblock %}
    {% block javascripts %}
        <script>
            function confirmaBorrar() {
                if (confirm("Desea Eliminar un Evento???")) {
                    //location.href
                }
            }
        </script>
    {% endblock %}
